{
  "name": "yakudoc-ai-prep",
  "version": "0.1.0",
  "description": "Generates placeholder-protected source files and a glossary so any LLM can translate yakudoc entries, then writes the results back",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "yakudoc-core": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
